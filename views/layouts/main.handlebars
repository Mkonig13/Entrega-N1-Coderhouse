<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Curso Node - Productos</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/modern-normalize/2.0.0/modern-normalize.min.css"
      integrity="sha512-+SCi+JmOFD+cmfwpay4kcxF5UeXdWtw36+Yp/MEo2vRcXZFd4qZ7ryO1G6Kr46PTkKSmY0Zs8jWw1kjJ3yP8Hg=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
    <style>
      body {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
        background: #0f172a;
        color: #e5e7eb;
        padding: 2rem;
      }
      a {
        color: #38bdf8;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
      }
      .nav-links {
        display: flex;
        gap: 1rem;
      }
      .card {
        background: #020617;
        border-radius: 0.75rem;
        padding: 1.5rem;
        box-shadow: 0 20px 45px rgba(15, 23, 42, 0.8);
        border: 1px solid #1f2937;
      }
      .btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.5rem 1rem;
        border-radius: 999px;
        border: none;
        cursor: pointer;
        font-size: 0.9rem;
      }
      .btn-primary {
        background: linear-gradient(to right, #22c55e, #16a34a);
        color: white;
      }
      .btn-danger {
        background: #b91c1c;
        color: white;
      }
      input,
      select {
        background: #020617;
        border-radius: 0.5rem;
        border: 1px solid #1f2937;
        color: #e5e7eb;
        padding: 0.4rem 0.6rem;
        width: 100%;
      }
      label {
        font-size: 0.8rem;
        color: #9ca3af;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>Productos - Curso Node</h1>
      <nav class="nav-links">
        <a href="/">Home</a>
        <a href="/realtimeproducts">Tiempo real</a>
      </nav>
    </header>

    {{{body}}}

    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/realtime.js"></script>
  </body>
</html>

